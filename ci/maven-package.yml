platform: linux

image_resource:
  type: docker-image
  source:
    repository: maven
    tag: '3.3-jdk-8'

inputs:
  - name: git-repo

#outputs:
#  - name: jar-file
#    path: git-repo/target/

run:
  path: mvn
  args: ["-f", "git-repo/pom.xml", "package"]